#include <stdio.h>

struct Car{
    char make[25];
    char model[25];
    int year;
    char color[15];
};

// this challenge was about just to decalare struct car with its make model year and color attributes
typedef struct Car Car;

int main()
{
    
    return 0;
}